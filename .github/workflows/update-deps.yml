name: Update Dependencies

# Disabled until bun2nix workflow issues are resolved
# on:
#   schedule:
#     # Run daily at 6am UTC
#     - cron: "0 6 * * *"
#   workflow_dispatch:

on: workflow_dispatch

permissions:
  contents: write
  pull-requests: write

jobs:
  update:
    uses: mholtzscher/github-actions/.github/workflows/update-deps.yml@main
    secrets:
      token: ${{ secrets.PAT }}
